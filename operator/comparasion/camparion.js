// camparion


// = ,
// == not strict equality,
// === strict equality,
// != // not equal
// !== // not strict equal
// <= // less than or equal to
// >= // greater than or equal to
// < // less than
// > // greater than



// = // Assigns the value of the right operand to the left operand // value daalna

// 12 == 13 // false equal hai kya wo dekhta hai, par iske sath ek problem hai ki agar type mismatch ho to wo convert kar deta hai
// 12 == '12' // true, kyuki type mismatch hai, par value same hai, isliye true


// 12 === 12 // true, type bhi check karta hai, value bhi check karta hai, dono same hone chahiye
// 12 === '12' // false, kyuki type mismatch hai


// 12 != 13 // true, equal nahi hai, value check karta hai
// 12 != '12' // false, kyuki value same hai, type mismatch hai, par wo value ko check karta hai

// 12 !== 12 // false, type bhi check karta hai, value bhi check karta hai, dono same hone chahiye
// 12 !== '12' // true, kyuki type mismatch hai, value bhi check karta hai, dono same hone chahiye

// 12 < 13 // true, less than hai

// 12 > 13 // false, greater than nahi hai