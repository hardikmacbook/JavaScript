let loggedIn = true;
let hasToken = false;


// && means dono true ho to hi chalega 

// isme eek hi condotion true hi to ye false hoga to hai : ke baad ka code chalega
let access = loggedIn && hasToken ? "Access Granted" : "Access Denied";
console.log(access);

// output: Access Denied

